# MyJSI App

React + Vite application for the MyJSI dealer experience.

## Scripts

```bash
npm install
npm run dev
npm test -- --run
npm run build
```

`npm run dev` starts the local Vite server. `npm run build` creates the production bundle in `dist/`.
